services:
  - type: web
    name: globetrotter-monolith
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        value: change-me-in-prod
